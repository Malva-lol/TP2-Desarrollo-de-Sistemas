model Sesion{
  id String @id @default(cuid())
  id_user String
  session_token String
  
  user Cliente @relation(fields: [id_user], references: [id])

  @@unique([id_user, session_token])
  @@map("sesiones")
}